import { AppointmentRepository } from '@domain/repositories/appointment/index';

describe('AppointmentRepository', () => {
  it('should be defined', () => {
    expect(AppointmentRepository).toBeDefined();
  });
});
