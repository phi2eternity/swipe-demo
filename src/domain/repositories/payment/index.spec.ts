import { PaymentRepository } from '@domain/repositories/payment/index';

describe('PaymentRepositoryImpl', () => {
    it('should be defined', () => {
      expect(PaymentRepository).toBeTruthy();
    });
});
