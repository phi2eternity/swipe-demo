
import {CapacityRepository} from "@domain/repositories/capacity-repository";

describe('CapacityRepository', () => {
  it('should be defined.', () => {
    expect(CapacityRepository).toBeDefined();
  });

});
