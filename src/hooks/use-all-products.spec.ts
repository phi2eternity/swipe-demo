import useAllProducts from '@hooks/use-all-products';

describe('useAllProducts', () => {
  it('should be defined.', () => {
    expect(useAllProducts).toBeDefined();
  });
  // TODO: implement more tests.

});
