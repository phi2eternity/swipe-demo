import useAvailableSlots from '@hooks/use-available-slots';

describe('useAvailableSlots', () => {
  it('should be defined.', () => {
    expect(useAvailableSlots).toBeDefined();
  });
  // TODO: implement more tests.

});
