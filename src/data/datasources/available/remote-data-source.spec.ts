import {AvailableRemoteDataSource} from "@data/datasources/available/remote-data-source";

describe('AvailableRemoteDataSource', () => {
  it('should be defined', () => {
    expect(AvailableRemoteDataSource).toBeDefined();
  });
});
