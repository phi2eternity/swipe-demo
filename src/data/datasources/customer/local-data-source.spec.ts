import { CustomerLocalDataSource } from '@data/datasources/customer/local-data-source';

describe('CustomerLocalDataSource', () => {
  it('should be defined', () => {
    expect(CustomerLocalDataSource).toBeDefined();
  });
});
