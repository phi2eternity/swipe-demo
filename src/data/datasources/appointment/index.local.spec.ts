
import { AppointmentLocalDataSource } from '@data/datasources/appointment/index.local';

describe('AppointmentLocalDataSource', () => {
  it('should be defined', () => {
    expect(AppointmentLocalDataSource).toBeDefined();
  });
});
