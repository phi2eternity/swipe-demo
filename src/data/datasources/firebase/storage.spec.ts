import { QuickerFirebaseStorage } from '@data/datasources/firebase/storage';

describe('QuickerFirebaseStorage', () => {
  it('should be defined', () => {
    expect(QuickerFirebaseStorage).toBeDefined();
  });
});
